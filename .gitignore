# WhatsApp chat export — keep raw chat data out of git
_chat.txt
*_chat.txt
_oldchat.txt
_oldhistory.txt
*history.txt

# wacli local store / secrets (never publish phone numbers or salt)
.wacli/
scoreboard_salt
player_names.json

# OS / editor cruft
.DS_Store
*.swp
__pycache__/
*.pyc

# stats are published on the scoreboard-data branch, not main
/docs/data/stats.json
